// metered-mcp — showcase app placeholder.
//
// Real work starts in Stage 1: a bare MCP server exposing one free tool, then
// (stage by stage) x402 paywall, a paying buyer agent, CCTP withdrawal, and
// spend guards — each need extracted into the `arcpayments` package first.
//
// Intentionally empty for Stage 0.
export const APP_NAME = "metered-mcp";
