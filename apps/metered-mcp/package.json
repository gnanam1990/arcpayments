{
  "name": "metered-mcp",
  "version": "0.0.1",
  "private": true,
  "description": "Showcase app (customer zero): a metered MCP tool server where an AI agent pays sub-cent USDC on Arc per tool call. Free echo + paid premium_echo (x402); buyer agent with a capped payment loop.",
  "license": "MIT",
  "type": "module",
  "bin": {
    "metered-mcp": "./dist/index.js"
  },
  "scripts": {
    "build": "tsup",
    "start": "bun run src/index.ts",
    "live:settle": "bun run scripts/live-settle.ts"
  },
  "dependencies": {
    "arcpayments": "workspace:*",
    "@modelcontextprotocol/sdk": "^1.29.0",
    "viem": "^2.54.1",
    "zod": "^4.4.3"
  },
  "engines": {
    "node": ">=20"
  }
}
