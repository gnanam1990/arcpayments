# ---- Arc network (fill from NETWORK.md after Stage 0 verification) ----
# TESTNET today. Switch to mainnet later by changing ONLY these values.
ARC_RPC_URL=            # e.g. https://rpc.testnet.arc.network  (VERIFY in Stage 0)
ARC_CHAIN_ID=           # e.g. 5042002                          (VERIFY in Stage 0)
ARC_EXPLORER_URL=       # e.g. https://testnet.arcscan.app      (VERIFY in Stage 0)

# ---- Faucet (testnet USDC/EURC) ----
# Get test funds at https://faucet.circle.com  (no key needed; here for reference)

# ---- Wallets (TESTNET ONLY — never commit real keys) ----
# Populated by `arcpayments wallet:new` in a later stage.
SELLER_PRIVATE_KEY=
BUYER_PRIVATE_KEY=

# ---- Payments (later stages) ----
# PAYWALL_PRICE_USDC=0.001
